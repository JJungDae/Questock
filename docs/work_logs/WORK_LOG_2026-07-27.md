# WORK_LOG_2026-07-27.md

## 1 — B9 / M4 Gate 최종 종료

### 계획
- 목적: B9 독립 검수 후속 조치, 병합 main quality-gate, Human Owner 확인을
  근거로 B9와 M4 Gate의 최종 상태를 동기화한다.
- 승인 범위: B9 Task Card, Source of Truth, 오늘자 작업 로그의 문서 동기화.

### 확인한 기존 상태
- 파일:
  - `docs/TASK_CARDS/B9-release-deployment-traceability.md`
  - `docs/agent_handoff/SOURCE_OF_TRUTH_INDEX.md`
  - `docs/agent_handoff/WORK_LOG_TEMPLATE.md`
- 함수·기능: 애플리케이션 코드 변경 없음.
- 미확인 사항: live provider·Gemini 연결과 실제 source coverage는 이번 종료
  범위가 아니다.

### 변경
- 수정 파일:
  - `docs/TASK_CARDS/B9-release-deployment-traceability.md`
  - `docs/agent_handoff/SOURCE_OF_TRUTH_INDEX.md`
  - `docs/work_logs/WORK_LOG_2026-07-27.md`
- 주요 diff:
  - closure 구현 SHA와 PR #7 merge/main SHA 기록
  - merged-main quality-gate PASS 기록
  - Human Owner 확인 기록
  - B9 `PASS / complete`, M4 Gate `PASS` 동기화
  - 다음 단계 `A15-M activation check` 기록

### 검증
| 명령·테스트 | 상태 | 실제 결과 |
|---|---|---|
| `git merge-base --is-ancestor 76d8ad2... origin/main` | PASS | exit 0 |
| `gh run list --commit c97f1af...` | PASS | quality-gate run `30210025937`, `success` |
| B9 closure local targeted pytest | PASS | 17 passed |
| B9 closure local full pytest | PASS | 1852 passed, 2 warnings |
| B9 closure M3 Gate | PASS | 34/34, Critical 17/17, public exposure 0 |
| B9 closure secret scan | PASS | `[]` |
| B9 closure compile | PASS | exit 0, no output |

### Git 작업
- commit: closure implementation
  `76d8ad2fc3a2565e022774333f1958ebbbae709f`
- push: `origin/fix/b9-m4-gate-closure` complete
- PR: #7 complete
- merge: `c97f1af461753c7d05fb8ed9a9f7365182d91f2b`
- 배포: 기존 recorded release deploy run `30207335981` PASS; 이번 문서
  동기화에서는 재배포 `NOT_RUN`

### 판단
- 완료 기준: B9 `PASS / complete`, M4 Gate `PASS`.
- 남은 위험: runtime은 recorded-only이며 live provider·Gemini connectivity와
  실제 source coverage는 보장하지 않는다. 공시 scenario는
  `partial`과 `insufficient_disclosure_coverage`를 유지한다. 성공한 배포에서
  rollback 실행은 `NOT_RUN`이다.
- fallback: 기존 recorded fixed-template fallback과 검증된 rollback target을
  유지한다.
- 다음 Step 진입: `A15-M activation check` 계획 수립만 허용.

### 사용자 확인
- 확인 시점: 2026-07-27
- 확인 결과: Human Owner 확인 완료. B9 최종 종료와 M4 Gate PASS 기록 승인.

## 2 — FSC-0 확인과 FSC-1 시작

### 계획
- 목적: First Service Completion bundle을 M4 이후 공식 흐름에 삽입하고
  credential·API·source preflight 결과를 확정한 뒤 FSC-1을 시작한다.
- 승인 범위: FSC 문서 상태 동기화, UTF-8 NAVER API HUB collector를 우선한
  FSC-1 SC-01~04 구현과 로컬 검증.

### FSC-0 확인 결과
- Human Owner 확인 시점: `2026-07-27`
- FSC-0 최종 상태: `PASS / complete`
- NAVER 최초 `0 items` 판정:
  `ENVIRONMENT_INVALID - PowerShell pipeline encoding이 한글 query를 ?로 치환`
- UTF-8 재검증:
  `PASS - 공식 예제·일반 query·3개 종목 query 모두 HTTP 200, total > 0,
  items·pubDate·originallink 확인`
- NAVER automatic collection:
  `COMPATIBLE`
- `2026-07-24` cutoff 기준 뉴스 15건 확보:
  `NOT_CONFIRMED`
- 뉴스 metadata 저장·가공 permission:
  `HUMAN_OWNER_REVIEW_REQUIRED`
- Gemini generation:
  `rate_limited - FSC-2 전 quota/billing 확인과 sanitized rerun 필요`

### FSC-0 검증
| 명령·테스트 | 상태 | 실제 결과 |
|---|---|---|
| 전체 pytest | PASS | 1852 passed, 2 warnings |
| M3 Gate | PASS | 34/34, Critical 17/17, public exposure 0 |
| FSC docs targeted | PASS | 99 passed |
| secret scan | PASS | `[]` |
| compile | PASS | exit 0 |

### FSC-1 시작
- 시작 승인: Human Owner 승인 완료.
- 현재 단계: `SC-01 UTF-8 NAVER API HUB collector first`.
- 구현 경계:
  - UTF-8로 저장된 Python script 사용
  - PowerShell here-string을 Python stdin으로 전달하지 않음
  - raw response는 Git 제외 경로에만 저장
  - 기사 전문 저장 금지
  - cutoff·회사 귀속·중복·permission 확인 전 actual coverage 주장 금지
- FSC-2·FSC-3: bundle 순서에 따라 `BLOCKED`.

### Git 작업
- commit: `NOT_RUN`
- push: `NOT_RUN`
- PR: `NOT_RUN`
- merge: `NOT_RUN`
- deploy: `NOT_RUN`

## 3 — FSC-1 SC-01·SC-02 결과 확인

### 완료 Step
- SC-01 뉴스 품질 재선별:
  `PASS / complete - Human Owner confirmed 2026-07-27`
- SC-02 공시 입력:
  `PASS / complete - Human Owner confirmed 2026-07-27`
- FSC-1 전체:
  `IN_PROGRESS - SC-03 report input/permission과 SC-04 snapshot runtime이 남음`

### 확인된 결과
- SC-01:
  종목별 5건, 장전/장중 `2/3, 1/4, 2/3`, Git ignored 검토표 유지.
- SC-02:
  고정 receipt별 1 document, facts `18 / 21 / 20`, 필수 category `10/10`,
  공식 viewer remark `유 / 유 / 유`, Git ignored 산출물 유지.
- 다음 checkpoint:
  `SC-03 report input 준비/검수`. Human Owner가 리포트 정리 완료를 알리기
  전에는 시작하지 않는다.

### 기존 구현 검증 기록
- 이 Step의 문서 동기화에서 코드 테스트를 새로 실행한 것으로 기록하지 않는다.
- 기존 실제 결과:
  targeted/integration `151 passed, 1 warning`, secret scanner targeted
  `82 passed`, Ruff/compile/secret scan/diff check `PASS`.

### 사용자 확인
- 확인 시점: `2026-07-27`
- 확인 결과:
  Human Owner가 SC-01 뉴스 품질 재선별과 SC-02 공시 구현 결과를 승인함.

### Git 작업
- commit: `NOT_RUN`
- push: `NOT_RUN`
- PR: `NOT_RUN`
- merge: `NOT_RUN`
- deploy: `NOT_RUN`

## 4 — FSC-1 SC-03 리포트 입력 구현

### 진행 Step
- 당시 상태:
  `IMPLEMENTED / Human Owner review pending`
- 당시 완료 기록 여부:
  `NO - Human Owner 결과 확인 전`
- 당시 FSC-1 전체:
  `IN_PROGRESS - SC-04 미시작`

### 입력·permission 확인
- Human Owner가 `2026-07-27`에 세 종목 리포트 package 준비를 알리고 다음
  범위를 승인함:
  - `usage_review_status=approved`
  - `corpus_ingest_allowed=true`
  - `external_llm_processing_allowed=false`
- 허용:
  PDF 검증 metadata, 구조화 fact, 투자의견·목표주가 attribution,
  value·page·section locator, Questock 자체 짧은 요약.
- 제외:
  source PDF, report body, evidence excerpt, raw text의 runtime·Git·Gemini
  사용.

### 구현 결과
- 추가 파일:
  - `app/services/report_snapshot_schema.py`
  - `scripts/curate_report_snapshot.py`
  - `tests/integration/test_service_snapshot_report.py`
- M1-06 corpus mode 재사용:
  `verified` hash와 `verified_against_source` document만 허용.
- 실제 corpus:
  삼성전자·SK하이닉스·현대자동차 각 12 section.
- PDF page:
  `8 / 9 / 12`.
- distinct verified locator page:
  `4 / 4 / 5`.
- 공식 PDF 기준 정정:
  삼성전자 NAND `+3%`, SK하이닉스 `HBM`, 현대차 발행일 `2026-07-24`.
- actual curation:
  `PASS`.
- 반복 build:
  `3 files byte-identical`.

### 검증
| 명령·테스트 | 상태 | 실제 결과 |
|---|---|---|
| SC-03 targeted pytest | PASS | 21 passed, 1 warning |
| SC-01~03 + M1-04~M1-06 regression | PASS | 382 passed, 1 warning |
| Ruff | PASS | All checks passed |
| compile | PASS | exit 0 |
| import smoke | PASS | `3 True` |
| secret scanner targeted pytest | PASS | 82 passed |
| secret scan | PASS | `[]` |
| curated forbidden-content/local-path scan | PASS | `NO_MATCH` |
| diff check | PASS | no whitespace errors |
| full regression | NOT_RUN | scoped source work |
| GitHub CI | NOT_RUN | - |

### 경계
- SC-03 완료 확인:
  `PASS / complete - Human Owner confirmed 2026-07-27`.
- SC-04·FSC-2·FSC-3:
  `SC-04 started by Human Owner instruction; FSC-2/FSC-3 not started`.
- commit·push·PR·merge·deploy:
  `NOT_RUN`.

## 5 — FSC-1 SC-03 확인과 SC-04 완료

### 사용자 확인
- SC-03:
  `PASS / complete - Human Owner confirmed 2026-07-27`.
- Human Owner가 SC-04 구현과 FSC-1 마감을 지시함.

### SC-04 구현
- snapshot:
  `svc-20260724-1402`.
- fixed basis:
  `2026-07-24T05:02:00Z`.
- canonical files:
  `manifest.json`, `documents.json`, `coverage_matrix.json`,
  `permission_register.json`.
- generated evidence:
  `snapshot_checksum.txt`, `validation_report.json`.
- runtime:
  명시된 snapshot ID에서만 새 immutable snapshot을 선택하고, snapshot ID가
  없으면 기존 B9 demo를 그대로 유지함.
- process singleton, fixed clock, fetch deep copy, sanitized invalid snapshot
  failure를 검증함.

### 실제 coverage
- supported securities:
  정확히 `KRX:005930`, `KRX:000660`, `KRX:005380`.
- news:
  `5 × 3 = 15`; pre-market/intraday `2/3, 1/4, 2/3`.
- disclosures:
  `1 × 3 = 3`; verified facts `18 / 21 / 20`.
- research reports:
  `1 × 3 = 3 manifests`; verified section documents `12 × 3 = 36`.
- total:
  `54 documents`, global IDs `54 / 54 unique`, basis 이후 `0`.
- documents SHA-256:
  `54a57430f228d0b6305fff979beefeed8da0ebcdfdcbcd92544cc17575bdcf83`.
- two-build identity:
  서로 다른 두 출력 디렉터리의 6개 파일 모두 byte-identical.

### 검증
| 명령·테스트 | 상태 | 실제 결과 |
|---|---|---|
| snapshot build | PASS | 54 documents |
| snapshot validator | PASS | exit 0 |
| SC-04 targeted + B9 runtime compatibility | PASS | 41 passed, 2 warnings |
| full regression | PASS | 1946 passed, 2 warnings |
| full Ruff | PASS | All checks passed |
| full compile | PASS | exit 0 |
| tracked secret/local-path scan | PASS | `[]` |
| diff check | PASS | no whitespace errors; line-ending warnings only |
| independent pytest rerun | NOT_RUN | - |
| GitHub CI | NOT_RUN | - |

### 실행 중 확인된 보완
- builder direct-file 호출의 import-path 실패 후 module 호출로 재실행함.
- 종목 간 공유된 provider news ID를 발견해 security-aware deterministic
  SHA-256 snapshot ID로 전환함.
- 일반 최근 이슈 질의 두 건의 low relevance를 project-owned 대표 질의
  label로 보완했으며 M2 retrieval 계약과 source content는 변경하지 않음.

### 최종 상태
- SC-04:
  `PASS / complete`.
- FSC-1:
  `PASS / complete - SC-01~04 complete`.
- FSC-2:
  계획·preflight만 `ALLOWED`; Gemini quota/billing 확인, 승인된 sanitized
  generation smoke, 별도 구현 승인 전 code implementation은 `BLOCKED`.
- FSC-3:
  `BLOCKED by bundle order`.
- commit·push·PR·merge·deploy:
  `NOT_RUN`.

## 6 — FSC-2 sanitized smoke와 SC-05 구현

### Human Owner 확인과 live smoke
- Human Owner billing 확인 및 승인된 smoke 실행:
  `CONFIRMED`.
- 실제 live Gemini 호출:
  `정확히 1회`.
- 안전한 결과:
  `status=ok`, `model=gemini/gemini-3.5-flash`,
  `structured_parse_ok=true`, `usage_present=true`, `finish_reason=stop`,
  `latency_ms=1854.106`.
- 호출 계약:
  strict JSON schema, `reasoning_effort=minimal`,
  provider `thinkingLevel=minimal`, max output `1024`, timeout `10`, retry `0`.
- 뉴스·공시·리포트·session·credential·로컬 경로 입력:
  `NONE`.
- raw prompt·raw provider response·raw provider error 출력 또는 기록:
  `NONE`.
- 후속 live Gemini 호출:
  `0회`.

### FSC-2 구현 결과
- runtime:
  `QUESTOCK_LLM_MODE=disabled|gemini`; 기본 `disabled`.
- request protection:
  client 10회/5분·50회/KST day, global concurrency 2·100회/KST day,
  1024 bucket·24시간 TTL.
- session:
  최근 4 exchange, user/assistant 각 2,000자, session 16,000자,
  LLM context 최근 2 exchange·4,000자.
- response cache:
  90초, global 256, session당 4, resulting revision, deep-copy.
- browser transcript:
  현재 session 최근 4건 deep-copy, 새 session에서 초기화, 명시적 form submit
  밖 transport 호출 없음.
- permission:
  외부 LLM 처리 permission이 없는 리포트는 fixed-only이며 quota를 쓰지 않음.
- deploy contract:
  API-only `.env.runtime`, mode 600 atomic install, env-first rollback.

### 검증
| 범위 | 결과 |
|---|---|
| FSC-2 targeted | `PASS - 227 passed, 2 warnings` |
| affected integration | `PASS - 73 passed, 2 warnings` |
| M3 Gate | `PASS - 34/34; Critical 17/17; public exposure 0` |
| full regression | `PASS - 1999 passed, 2 warnings` |
| Ruff | `PASS` |
| compile | `PASS` |
| secret/local-path scan | `PASS - []` |
| diff check | `PASS` |
| GitHub CI | `NOT_RUN` |
| independent pytest rerun | `NOT_RUN` |
| deploy | `NOT_RUN` |

첫 affected integration 실행은 Windows 임시 폴더 접근 오류로 결과가
무효였으며, 동일 선택을 별도 임시 경계에서 재실행해 `73 passed`를 확인함.

### 현재 상태
- FSC-2:
  `PASS / complete - Human Owner confirmed 2026-07-27`.
- Human Owner 구현 결과 확인:
  `PASS`.
- FSC-3:
  `SC-06 planning/implementation ALLOWED; SC-07 NOT_RUN`.
- FSC-2 Git:
  commit/push `25cac10030800f08d4167b9f7739d06b3d1492ca` / complete;
  Human Owner-created PR #10
  `https://github.com/JJungDae/Questock/pull/10` / `MERGED`;
  merge SHA `92561e34b4839d32a9bdac979c6c471da8e56923`.
- SC-06 onward commit·push·PR update·merge·deploy:
  `NOT_RUN`.

## 7 — FSC-2 결과 확인과 FSC-3 진입

- Step status:
  `PASS / complete - Human Owner confirmed 2026-07-27`.
- 확인한 local branch/HEAD/upstream:
  `fsc/fsc-0-preflight` /
  `25cac10030800f08d4167b9f7739d06b3d1492ca` /
  `origin/fsc/fsc-0-preflight` at the same SHA.
- 확인한 GitHub PR:
  `#10`, `MERGED`,
  `https://github.com/JJungDae/Questock/pull/10`;
  head SHA `25cac10030800f08d4167b9f7739d06b3d1492ca`;
  merge SHA `92561e34b4839d32a9bdac979c6c471da8e56923`.
- working tree baseline:
  existing untracked `.tmp/` preserved; no destructive Git operation.
- next checkpoint:
  `FSC-3 / SC-06 UI and versioned acceptance fixture`.
- live Gemini calls:
  `0 additional calls`; the previously approved single smoke was not repeated.
- SC-07 / commit / push / PR update / merge / deploy:
  `NOT_RUN`.

## 8 — FSC-3 / SC-06 로컬 구현

- Step status:
  `IMPLEMENTED / local verification PASS WITH REQUIRED FOLLOW-UP`;
  Human Owner result confirmation `PENDING`.
- UI:
  지원 종목 selector 제거, 고정 snapshot display-only 상태와 기준시각
  `2026-07-24 14:02 KST`, recorded mode, 생성/fallback, report permission,
  disclosure coverage, quota fallback, 개인 금융정보 입력 금지 표시.
- acceptance fixture:
  `tests/fixtures/service_acceptance/fsc_v1.json`; exact 15 questions,
  12 LLM-eligible, 3 Critical; strict schema/validator/static tests.
- representative local API/UI:
  삼성전자·SK하이닉스·현대자동차 recorded/fixed flow `PASS`.
- targeted:
  `32 passed, 2 warnings`.
- affected regression:
  `266 passed, 2 warnings`.
- full regression:
  `2020 passed, 2 warnings`.
- M3 Gate:
  `34/34`; Critical `17/17`; public exposure `0`.
- Ruff / compile / secret scans / JSON / diff check:
  `PASS / PASS / [] / PASS / PASS`.
- additional live Gemini calls:
  `0`.
- 15-case live acceptance:
  `NOT_RUN - separate Human Owner approval required`.
- required follow-up before live acceptance:
  exact dated recent-issue 3건 lexical miss, Korean `2025년 1월` 기간 미해석,
  risk/multi-source response Evidence inventory, V4 12-case LLM eligibility와
  fixed-only report permission 경계.
- SC-07 / commit / push / PR update / merge / deploy:
  `NOT_RUN`.

## 9 — FSC-3 / SC-06 일반화 보완

- Step status:
  `IMPLEMENTED / focused root supplement local PASS / live acceptance READY`;
  Human Owner result confirmation `PENDING`.
- lexical retrieval:
  query/document에 동일한 결정적 Hangul 조사·요청형 suffix 정규화를 적용;
  BM25 threshold `0.5`, hard filter, score formula, wrong-company 계약 유지.
- period:
  strict `YYYY년 M월`, `YYYY년 M월 D일`; 월말·윤년 계산, 잘못된 월/일 거부,
  기존 ISO/session 기간 계약과 조합.
- multi-source:
  threshold를 넘은 Evidence 안에서 required source별 대표를 먼저 보존하고
  나머지를 score 순으로 채움; 없는 source를 강제 포함하지 않음.
- projection:
  retrieval/context budget/fixed composer가 같은 source-diverse helper를
  사용하며 public Evidence는 citation-bound subset 유지.
- permission:
  report external processing false 유지; mixed LLM prompt report Evidence 0;
  report-only LLM call 0; mixed composition 미추가.
- acceptance:
  required Evidence source를 policy `satisfied_sources`로 검증; exact 15
  questions와 canonical snapshot content는 변경하지 않음.
- snapshot:
  54 documents validator PASS; 두 build byte identity 및 tracked canonical
  identity PASS; document checksum
  `54a57430f228d0b6305fff979beefeed8da0ebcdfdcbcd92544cc17575bdcf83`.
- focused targeted:
  `347 passed, 2 warnings`.
- M3 Gate tests:
  `9 passed, 1 warning`.
- full regression:
  `2048 passed, 2 warnings`.
- M3 Gate:
  `34/34`; Critical `17/17`; public exposure `0`.
- additional live Gemini/provider calls:
  `0`.
- 15-case live acceptance:
  `NOT_RUN - separate Human Owner approval required`.
- SC-07 / commit / push / PR update / merge / deploy:
  `NOT_RUN`.

## 10 — FSC-3 / SC-06 live acceptance

- Step status:
  `PASS / complete`.
- 승인된 provider attempt 상한:
  `30`.
- 실제 provider attempts:
  `26`; 잔여 `4`.
- batch 결과:
  primary `2/12`, first focused `5/10`, final focused `3/4`.
- 누적 LLM 성공:
  `10/12`.
- 조기 중단:
  final focused batch에서 누적 기준 도달 직후 남은 FSC-11 재호출 없이
  중단.
- 안전 fallback:
  `FSC-02`, `FSC-11`.
- Critical:
  `3/3 PASS`; Gemini call `0`.
- 전체 public validation:
  `15/15 PASS`.
- unsupported number / wrong-company / uncited core number / direct advice:
  `0 / 0 / 0 / 0`.
- report 원문·요약·metadata snippet Gemini 전송:
  `0`.
- raw prompt·response·credential·raw error·로컬 경로 기록:
  `NONE`.
- commit / push / PR / merge / deploy:
  `NOT_RUN`.

## 11 — FSC-3 / SC-07 로컬 release 준비

- Step status:
  `IMPLEMENTED / local non-Docker gates PASS`.
- CI:
  public model allowlist, adapter, request protection, bounded session/cache,
  snapshot checksum, acceptance fixture, rerun/F5 no-call, secret/rollback
  계약을 explicit focused gate로 추가.
- GCE runtime:
  recorded snapshot, Gemini 3.5 minimal, request protection/cache, timeout 10,
  output 1024 계약 유지.
- secret/rollback:
  API-only `.env.runtime`, atomic mode-600 install, environment-first
  image/SHA rollback 유지.
- focused release contracts:
  `139 passed, 1 warning`.
- claim/citation targeted:
  `127 passed, 2 warnings`.
- citation/ChatService/adapter/context regression:
  `302 passed, 1 warning`.
- final focused:
  `54 passed, 1 warning`.
- full regression:
  `2062 passed, 2 warnings`.
- M3 Gate:
  `34/34`; Critical `17/17`; public exposure `0`.
- snapshot:
  validator `54 documents`; two builds and tracked canonical six files
  byte-identical.
- Ruff / compile / secret-local-path:
  `PASS / PASS / []`.
- Docker clean build / API/UI health:
  `BLOCKED - Docker client는 존재하지만 local Docker engine이 실행 중이지
  않음`.
- GitHub CI / GCE remote smoke / exact-SHA deployment:
  `NOT_RUN / NOT_RUN / NOT_RUN`.
- FSC-3 전체:
  `IN_PROGRESS`.
- commit / push / PR / merge / deploy:
  `NOT_RUN / NOT_RUN / NOT_RUN / NOT_RUN / NOT_RUN`.

## 12 — FSC-3 / SC-07 Docker checkpoint (remote closure 전)

- Step status:
  `PASS - local Service Completion gate`.
- Docker engine:
  `PASS - server 29.6.2`.
- clean locked build:
  `PASS - questock:fsc-sc07-local-25cac100`;
  image `sha256:ad8d753d78a0b84fa3321f225188d567fc1940df5e5dac38c6b075ff50384bf4`.
- container security:
  non-root `questock`, read-only rootfs, all capabilities dropped,
  `no-new-privileges` 모두 `PASS`.
- API health:
  `HTTP 200`; snapshot `svc-20260724-1402`; recorded documents `54`.
- UI:
  health `HTTP 200`; root HTML `HTTP 200`.
- recorded release smoke:
  `PASS - 7/7`; recent issue `complete`, disclosure `partial`, report
  `complete`, glossary `complete`, wrong-company `no_evidence`, advice
  `blocked`, multi-turn `partial`.
- focused regression:
  `28 passed, 1 warning`; Ruff와 compile `PASS`.
- 확인된 보완:
  첫 smoke는 B9 기준일과 SK하이닉스 공시 부재를 전제로 한 오래된
  release-smoke assertion 때문에 실패했다. FSC 기준일, versioned
  wrong-company 문항, 확장 공시 fact의 값·단위·physical PDF page·DART
  printed page·section 포함 검증으로 최소 수정한 뒤 재실행해 통과했다.
- additional Gemini/provider attempts:
  `0`; 누적 `26/30` 유지.
- local Service Completion gate:
  `PASS`.
- 전체 Service Completion Gate:
  `NOT_RUN - GitHub CI, GCE remote smoke, exact-SHA deployment, external UI
  확인이 남아 있음`.
- FSC-3:
  `LOCAL CHECKPOINT PASS / remote release closure pending`.
- commit / push / PR / merge / deploy:
  `NOT_RUN / NOT_RUN / NOT_RUN / NOT_RUN / NOT_RUN`.

## 13 — FSC-3 / SC-07 remote release closure

- Step status:
  `PASS / complete`.
- PR #11:
  head `8d1b9521fdbef003cb84708b6aa7b47d01d8dc8a`; first CI
  `30273039760` failed on the Python 3.11 Streamlit extracted-helper
  annotation; fix `8d1b952`; rerun `30273607080` passed; merge
  `6affd27f4f95aae438268acd2bc4fa7733346b5d`.
- first deploy:
  run `30273898079` failed before runtime write/deploy because the anchored
  Gemini-key allowlist excluded one dot in the already-live-verified
  53-character key. The existing service/image remained unchanged and
  rollback execution was `NOT_RUN`.
- PR #12:
  commits `499bf03`, `bed5684`; first CI `30274239625` failed because the
  legacy B9 static expectation retained the old regex; synchronized focused
  contracts passed `139`; rerun `30274469379` passed; merge
  `2adcc787a803996d4a181a6cd3faa3158602660a`.
- exact-SHA deploy:
  run `30274651799` passed in `3m32s`; release SHA
  `2adcc787a803996d4a181a6cd3faa3158602660a`; image
  `sha256:53628bacc40f2329bc3f7dfcb6771aeee2e5fd83a1a44592ee08bbc950daf138`.
- rollback target:
  SHA `67fa43dd5a7ec74e7785713eb1adcfa402baab85`; image
  `sha256:56df8f16ed3ed58de659e9ec46c9e24b7d3ddc896dc8a022102f68f351d7b928`;
  rollback execution `NOT_RUN - deployment passed`.
- runtime health:
  `PASS`; status ok, recorded snapshot `svc-20260724-1402`, 54 documents;
  news `15`, disclosure `3`, research report `36`;
  `live_connectivity_checked=false`.
- remote recorded smoke:
  `PASS - 7/7`; recent issue `complete`, disclosure `partial`, research report
  `complete`, glossary `complete`, wrong-company `no_evidence`, blocked
  `blocked`, multi-turn `partial`.
- UI:
  workflow external health `PASS`; a separate reviewer browser attempt to the
  registered host on port 8501 timed out, so interactive visual rendering is
  `NOT_CLAIMED`. This is a non-gate independent-check limitation, not a
  deployment failure.
- Gemini:
  deployment smoke used the fixed/disabled recorded path; additional calls
  `0`; cumulative provider attempts `26/30`.
- final boundary:
  FSC-3 `PASS / complete`; Service Completion Gate `PASS / complete`; remote
  release closure `complete`; release commit/push/PR/merge/deploy `complete`.
- next:
  A15-M activation and entry `READY / ALLOWED`; implementation
  `NOT_STARTED`. Stretch M2-09, M5-01, and later P1 ordering unchanged.
